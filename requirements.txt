#this allows us to have those responsive notebooks
jupyter

# Among many uses, will convert CSV data into a spreadsheet
pandas

# the tool for data visualization
matplotlib

# enables interactive widgets and responsive plots?
ipympl

#if nothing else, has the utility for splitting the original dataframe into training vs. testing subsets
scikit-learn

#Needs this for stratified sampling or any large data moves like sorting, indexing or shuffling
numpy

# Need Pickle or JobLib so that Jupyter can store pipelines and re-use them in other notebooks
#joblib appears to be redundant because it was most likely baked into scikit-learn's installation